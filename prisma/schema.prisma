generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Data User (Pemain)
model User {
  id            Int      @id @default(autoincrement())
  firebaseUid   String?  @unique
  username      String   @unique
  role          String   @default("siswa")
  coins         Int      @default(0)
  totalScore    Int      @default(0)
  xp            Int      @default(0) // Poin pengalaman
  level         Int      @default(0) // Level pemain
  photoURL      String?
  inventory     Json     @default("[\"default\"]") // Barang yang dimiliki
  activeTheme   String   @default("default")
  equippedFrame String   @default("default")
  equippedBadge String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  scores        Score[]
}


// Data Game
model Game {
  id          Int     @id @default(autoincrement())
  slug        String  @unique // ID unik game (misal: math, zuma)
  title       String
  description String?
  scores      Score[]
}

// Riwayat Skor
model Score {
  id       Int      @id @default(autoincrement())
  score    Int
  playedAt DateTime @default(now())
  userId   Int
  gameId   Int
  game     Game     @relation(fields: [gameId], references: [id])
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// Bank Soal
model GameQuestion {
  id        Int      @id @default(autoincrement())
  category  String
  level     String
  content   Json
  createdAt DateTime @default(now())

  @@index([category, level])
}
